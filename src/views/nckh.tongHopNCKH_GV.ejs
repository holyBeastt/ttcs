<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Học Viện Kỹ Thuật Mật Mã - Tổng Hợp Số Tiết NCKH Dự Kiến (V2)</title>
    <link rel="stylesheet" href="/css/admin.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" />
    <link rel="stylesheet" href="/css/styles.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" />
    <link rel="stylesheet" href="/css/nckh.css" />
    <!-- AG-Grid CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/ag-grid-community@31.0.0/styles/ag-grid.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/ag-grid-community@31.0.0/styles/ag-theme-alpine.css" />
    <style>
        #suggestions {
            border: 1px solid #ccc;
            max-height: 150px;
            overflow-y: auto;
            position: absolute;
            width: 300px;
            background-color: #fff;
            z-index: 1000;
            display: none;
        }

        #suggestions div {
            padding: 5px;
            cursor: pointer;
        }

        #suggestions div:hover {
            background-color: #e0e0e0;
        }

        /* Custom Modal Styles (No Bootstrap) */
        .custom-modal-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6);
            z-index: 9999;
            justify-content: center;
            align-items: center;
        }

        .custom-modal-overlay.show {
            display: flex;
        }

        .custom-modal {
            background: #fff;
            border-radius: 8px;
            width: 95%;
            max-width: 1400px;
            max-height: 90vh;
            display: flex;
            flex-direction: column;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
            animation: modalSlideIn 0.3s ease;
        }

        @keyframes modalSlideIn {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .custom-modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 16px 20px;
            background: linear-gradient(135deg, #007bff, #0056b3);
            color: #fff;
            border-radius: 8px 8px 0 0;
            position: relative;
        }

        .custom-modal-header h5 {
            margin: 0;
            font-size: 18px;
            font-weight: 600;
            flex: 1;
        }

        .custom-modal-close {
            position: absolute;
            top: 8px;
            right: 8px;
            background: rgba(255, 255, 255, 0.2);
            border: none;
            color: #fff;
            font-size: 20px;
            cursor: pointer;
            width: 32px;
            height: 32px;
            border-radius: 4px;
            display: flex;
            align-items: center;
            justify-content: center;
            line-height: 1;
            opacity: 0.9;
            transition: all 0.2s;
        }

        .custom-modal-close:hover {
            opacity: 1;
            background: rgba(255, 255, 255, 0.3);
        }

        .custom-modal-body {
            flex: 1;
            padding: 20px;
            overflow: hidden;
        }

        .custom-modal-footer {
            display: flex;
            justify-content: flex-end;
            padding: 12px 20px;
            background: #f8f9fa;
            border-radius: 0 0 8px 8px;
            border-top: 1px solid #e9ecef;
        }

        .custom-btn {
            padding: 8px 24px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 14px;
            font-weight: 500;
            transition: all 0.2s;
            min-width: 80px;
        }

        .custom-btn-secondary {
            background: #6c757d;
            color: #fff;
        }

        .custom-btn-secondary:hover {
            background: #5a6268;
        }

        #detailGridContainer {
            height: 60vh;
            width: 100%;
        }

        /* AG-Grid custom styles */
        .ag-theme-alpine .ag-header-cell-label {
            font-weight: 600;
        }

        .ag-theme-alpine .ag-row:hover {
            background-color: #e8f4fd !important;
        }

        .ag-theme-alpine .ag-header-cell {
            border-right: 1px solid #babfc7 !important;
        }

        .ag-theme-alpine .ag-cell {
            border-right: 1px solid #dde2eb !important;
        }

        .ag-theme-alpine .ag-row {
            border-bottom: 1px solid #dde2eb !important;
        }

        .ag-theme-alpine .ag-header {
            border-bottom: 2px solid #babfc7 !important;
        }

        .ag-theme-alpine .ag-root-wrapper {
            border: 1px solid #babfc7 !important;
        }

        .cell-wrap-text {
            white-space: normal !important;
            line-height: 1.4;
        }

        .badge-role {
            display: inline-block;
            padding: 4px 8px;
            background: #6c757d;
            color: #fff;
            border-radius: 4px;
            font-size: 12px;
        }

        .badge-hours {
            display: inline-block;
            padding: 4px 10px;
            background: #28a745;
            color: #fff;
            border-radius: 4px;
            font-size: 13px;
            font-weight: 600;
        }
    </style>
</head>

<body>
    <!-- Phần header -->
    <%- include('header') %>

        <div class="my-3 box formNckh">
            <h2 class="titleNckh">Tổng hợp số tiết NCKH theo GV</h2>

            <!-- view tab  -->
            <div id="viewTab" class="tab-content py-4">
                <h2 class="titleNckh"></h2>
                <div class="flex-container-view">
                    <div class="flex-item">
                        <label for="namHocXem">Chọn Năm Học:</label>
                        <select id="namHocXem" class="namHoc"></select>
                    </div>
                    <div class="flex-item">
                        <input type="text" id="TenGiangVien" class="MaPhongBan" style="width: 300px; height: 35px;"
                            placeholder="Nhập tên giảng viên">
                        <div id="suggestions"></div>
                    </div>
                    <div class="flex-item-view">
                        <button onclick="" class="btn text-nowrap" id="render">Hiển thị</button>
                    </div>
                </div>

                <div class="table-container">
                    <div id="table-container"></div>
                </div>
            </div>
        </div>

        <!-- Custom Modal: Chi tiết hạng mục NCKH (No Bootstrap) -->
        <div class="custom-modal-overlay" id="detailModal">
            <div class="custom-modal">
                <div class="custom-modal-header">
                    <h5><i class="fas fa-list-ul"></i> Chi tiết: <span id="modalCategoryName"></span></h5>
                    <button class="custom-modal-close" onclick="closeDetailModal()">&times;</button>
                </div>
                <div class="custom-modal-body">
                    <div id="detailGridContainer" class="ag-theme-alpine"></div>
                </div>
                <div class="custom-modal-footer">
                    <button class="custom-btn custom-btn-secondary" onclick="closeDetailModal()">Đóng</button>
                </div>
            </div>
        </div>

        <!-- SweetAlert2 CSS -->
        <link href="https://cdn.jsdelivr.net/npm/sweetalert2@11.6.16/dist/sweetalert2.min.css" rel="stylesheet" />
        <!-- SweetAlert2 JavaScript -->
        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.6.16/dist/sweetalert2.all.min.js"></script>

        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
        <!-- AG-Grid JS -->
        <script src="https://cdn.jsdelivr.net/npm/ag-grid-community@31.0.0/dist/ag-grid-community.min.js"></script>

        <script src="/js/nckh_v2/utils.js"></script>

        <script>
            let currentCategoryData = {};
            let detailGridApi = null;

            document.getElementById("render").addEventListener("click", async function () {
                const year = document.getElementById("namHocXem").value;
                const teacher = document.getElementById("TenGiangVien").value.trim();
                if (!teacher) {
                    Swal.fire({ icon: 'warning', title: 'Cảnh báo', text: 'Vui lòng nhập tên giảng viên' });
                    return;
                }

                Swal.fire({ title: 'Đang tải...', didOpen: () => Swal.showLoading(), allowOutsideClick: false });

                try {
                    const response = await fetch(`/v2/tonghopsotiet/dukien/${year}`, {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ TenGiangVien: teacher })
                    });
                    const rs = await response.json();
                    Swal.close();

                    if (!rs.success) {
                        Swal.fire({ icon: 'error', title: 'Lỗi', text: rs.message });
                        return;
                    }

                    const { name: teacherName, total, tables: categories } = rs.data;
                    currentCategoryData = categories;

                    const keys = Object.keys(categories);
                    let tableHtml = '<table class="table table-bordered table-striped w-100">';
                    tableHtml += '<thead class="table-primary text-center"><tr><th>Họ tên</th><th>Hạng mục</th><th>Số tiết bộ phận</th><th>Tổng số tiết</th><th>Chi tiết</th></tr></thead><tbody>';

                    if (keys.length > 0) {
                        keys.forEach((key, index) => {
                            tableHtml += '<tr>';
                            if (index === 0) tableHtml += `<td rowspan="${keys.length}" class="text-center align-middle fw-bold">${teacherName}</td>`;
                            tableHtml += `<td>${key}</td><td class="text-center">${categories[key].totalHours}</td>`;
                            if (index === 0) tableHtml += `<td rowspan="${keys.length}" class="text-center align-middle fw-bold text-danger fs-4">${total}</td>`;
                            tableHtml += `<td class="text-center"><button onclick="viewCategoryDetail('${key}')"><i class="fa fa-eye"></i> Xem</button></td></tr>`;
                        });
                    } else {
                        tableHtml += `<tr><td class="text-center align-middle fw-bold">${teacherName}</td><td colspan="4" class="text-center py-4 text-muted">Không có dữ liệu cho năm học này</td></tr>`;
                    }
                    tableHtml += '</tbody></table>';
                    document.getElementById("table-container").innerHTML = tableHtml;

                } catch (e) {
                    Swal.fire({ icon: 'error', title: 'Lỗi', text: 'Không thể tải dữ liệu' });
                }
            });

            // Hàm mở modal
            function openDetailModal() {
                document.getElementById('detailModal').classList.add('show');
                document.body.style.overflow = 'hidden';
            }

            // Hàm đóng modal
            function closeDetailModal() {
                document.getElementById('detailModal').classList.remove('show');
                document.body.style.overflow = '';
            }

            // Đóng modal khi click overlay
            document.getElementById('detailModal').addEventListener('click', function(e) {
                if (e.target === this) closeDetailModal();
            });

            // Đóng modal khi nhấn ESC
            document.addEventListener('keydown', function(e) {
                if (e.key === 'Escape') closeDetailModal();
            });

            // Hàm tạo thông tin chi tiết dựa theo loại bảng
            function getExtraInfo(record) {
                const t = record.tableName;
                let info = [];
                
                if (t === 'detaiduan') {
                    if (record.MaSoDeTai) info.push(`Mã số: ${record.MaSoDeTai}`);
                    if (record.CapDeTai) info.push(`Cấp: ${record.CapDeTai}`);
                    if (record.ChuNhiem) info.push(`CN: ${record.ChuNhiem}`);
                } else if (t === 'baibaokhoahoc') {
                    if (record.LoaiTapChi) info.push(`Tạp chí: ${record.LoaiTapChi}`);
                    if (record.ChiSoTapChi) info.push(`Chỉ số: ${record.ChiSoTapChi}`);
                } else if (t === 'sangkien') {
                    if (record.CapSangKien) info.push(`Cấp: ${record.CapSangKien}`);
                    if (record.XepLoai) info.push(`Xếp loại: ${record.XepLoai}`);
                } else if (t === 'sachvagiaotrinh') {
                    if (record.LoaiSach) info.push(`Loại: ${record.LoaiSach}`);
                    if (record.NhaXuatBan) info.push(`NXB: ${record.NhaXuatBan}`);
                } else if (t === 'bangsangchevagiaithuong') {
                    if (record.Loai) info.push(`Loại: ${record.Loai}`);
                } else if (t === 'huongdansvnckh') {
                    if (record.LoaiHinhHuongDan) info.push(`Loại: ${record.LoaiHinhHuongDan}`);
                    if (record.SoSinhVien) info.push(`Số SV: ${record.SoSinhVien}`);
                } else if (t === 'thanhVienHoiDong') {
                    if (record.LoaiHoiDong) info.push(`Loại HĐ: ${record.LoaiHoiDong}`);
                    if (record.VaiTroHoiDong) info.push(`Vai trò HĐ: ${record.VaiTroHoiDong}`);
                }
                
                return info.join(' | ');
            }

            function viewCategoryDetail(categoryName) {
                const data = currentCategoryData[categoryName];
                document.getElementById('modalCategoryName').textContent = categoryName;

                // Chuẩn bị dữ liệu cho AG-Grid
                const rowData = data.records.map((record, index) => ({
                    stt: index + 1,
                    ten: record.ten,
                    vaiTro: record.vaiTro,
                    soTiet: record.soTiet,
                    namHoc: record.NamHoc,
                    thanhVien: record.DanhSachThanhVien || '',
                    thongTinThem: getExtraInfo(record)
                }));

                // Cấu hình cột cho AG-Grid
                const columnDefs = [
                    { 
                        headerName: 'STT', 
                        field: 'stt', 
                        width: 70, 
                        cellStyle: { textAlign: 'center' },
                        pinned: 'left'
                    },
                    { 
                        headerName: 'Tên nội dung NCKH', 
                        field: 'ten', 
                        flex: 2,
                        minWidth: 250,
                        wrapText: true,
                        autoHeight: true,
                        cellClass: 'cell-wrap-text'
                    },
                    { 
                        headerName: 'Vai trò', 
                        field: 'vaiTro', 
                        width: 130,
                        cellStyle: { textAlign: 'center' }
                    },
                    { 
                        headerName: 'Số tiết', 
                        field: 'soTiet', 
                        width: 100,
                        cellStyle: { textAlign: 'center' }
                    },
                    { 
                        headerName: 'Năm học', 
                        field: 'namHoc', 
                        width: 110,
                        cellStyle: { textAlign: 'center' }
                    },
                    { 
                        headerName: 'Thông tin thêm', 
                        field: 'thongTinThem', 
                        flex: 1,
                        minWidth: 200,
                        wrapText: true,
                        autoHeight: true,
                        cellClass: 'cell-wrap-text'
                    },
                    { 
                        headerName: 'Thành viên', 
                        field: 'thanhVien', 
                        flex: 1,
                        minWidth: 200,
                        wrapText: true,
                        autoHeight: true,
                        cellClass: 'cell-wrap-text'
                    }
                ];

                // Cấu hình AG-Grid
                const gridOptions = {
                    columnDefs: columnDefs,
                    rowData: rowData,
                    defaultColDef: {
                        sortable: true,
                        filter: true,
                        resizable: true
                    },
                    animateRows: true,
                    rowSelection: 'single',
                    domLayout: 'normal',
                    suppressHorizontalScroll: false,
                    onGridReady: (params) => {
                        detailGridApi = params.api;
                        params.api.sizeColumnsToFit();
                    },
                    onFirstDataRendered: (params) => {
                        params.api.sizeColumnsToFit();
                    }
                };

                // Xóa grid cũ nếu có và tạo mới
                const gridContainer = document.getElementById('detailGridContainer');
                gridContainer.innerHTML = '';
                agGrid.createGrid(gridContainer, gridOptions);

                openDetailModal();
            }
        </script>

        <!-- suggest giảng viên  -->
        <script>
            let giangVienList = [];

            // Gọi API để lấy danh sách giảng viên (Sử dụng API V2 hoặc V1 đều được, ở đây dùng V1 cho tiện vì đã có)
            // Hoặc dùng route mới /v2/giang-vien-co-huu
            async function fetchGiangVien() {
                try {
                    const response = await fetch('/giangviencohuu');
                    if (!response.ok) throw new Error('Lỗi khi lấy dữ liệu giảng viên');
                    giangVienList = await response.json();
                } catch (error) {
                    console.error('Lỗi:', error);
                }
            }

            // Hiển thị gợi ý
            function showSuggestions(filtered) {
                const suggestionsBox = document.getElementById('suggestions');
                suggestionsBox.innerHTML = '';

                if (filtered.length === 0) {
                    suggestionsBox.style.display = 'none';
                    return;
                }

                filtered.forEach(item => {
                    const div = document.createElement('div');
                    div.textContent = item.HoTen;
                    div.onclick = () => {
                        document.getElementById('TenGiangVien').value = item.HoTen;
                        suggestionsBox.style.display = 'none';
                    };
                    suggestionsBox.appendChild(div);
                });

                suggestionsBox.style.display = 'block';
            }

            // Xử lý sự kiện nhập
            document.getElementById('TenGiangVien').addEventListener('input', function () {
                const inputVal = this.value.toLowerCase();
                if (!inputVal) {
                    document.getElementById('suggestions').style.display = 'none';
                    return;
                }
                const filtered = giangVienList.filter(gv => gv.HoTen.toLowerCase().includes(inputVal));
                showSuggestions(filtered);
            });

            // Ẩn gợi ý khi click bên ngoài
            document.addEventListener('click', function (event) {
                if (!event.target.closest('#TenGiangVien') && !event.target.closest('#suggestions')) {
                    document.getElementById('suggestions').style.display = 'none';
                }
            });

            // Gọi API khi tải trang
            window.addEventListener('DOMContentLoaded', fetchGiangVien);
        </script>
        <!-- link href -->
        <script src="/js/nckh/href.js"></script>
        <!-- srcipt năm học, khoa -->
        <script src="/js/nckh/getdata.js"></script>

</body>

</html>